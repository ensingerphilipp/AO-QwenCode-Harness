# Project Semantic Review Rules

This file contains only repository-specific semantic invariants that are important during review and are not fully enforced by `bash scripts/verify`.

Global semantic-review execution, result validity, effort selection, disposition, repair routing, and publication rules are defined by the installed harness and must not be duplicated here.

Project facts should be defined in `PROJECT.md` or `ARCHITECTURE.md` and referenced here rather than restated when possible.

## Review invariants

{{PROJECT_REVIEW_INVARIANTS}}
