{
  "schemaVersion": 1,
  "id": "python",
  "description": "Python repositories whose formatter, linter, type checker, test, and build commands are derived from project configuration.",
  "detectionSignals": [
    "pyproject.toml",
    "setup.cfg",
    "tox.ini",
    "requirements files"
  ],
  "requiredInspectionFacts": [
    "PYTHON_VERSION",
    "PYTHON_ENVIRONMENT_COMMANDS",
    "PYTHON_VERIFICATION_COMMANDS"
  ],
  "requiredRenderTokens": [
    "PYTHON_VERIFICATION_COMMANDS",
    "PYTHON_VERSION"
  ],
  "verificationCandidates": [
    {
      "id": "project-checks",
      "includeWhen": "commands are configured by the repository or explicitly approved",
      "template": "{{PYTHON_VERIFICATION_COMMANDS}}"
    }
  ],
  "ciSetupCandidates": [
    {
      "id": "setup-python",
      "includeWhen": "GitHub-hosted CI must install the repository Python version",
      "template": "uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0\nwith:\n  python-version: {{PYTHON_VERSION}}"
    }
  ]
}
